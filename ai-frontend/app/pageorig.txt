// Add this state near the top of Home() function:
const [audioMessageId, setAudioMessageId] = useState<string | null>(null);

// Update the handleChatSubmit function:


// Update the message rendering logic:
